# Gap tests

These tests **document and identify** gaps in the codebase. Each test **fails** until the corresponding gap is implemented; see `issues/issueN/` for the issue description and ~2.5h implementation scope.

Run: `npm test -- __tests__/gaps`
