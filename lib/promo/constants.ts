/** Default minimum order (USD) when not specified for a promo. */
export const DEFAULT_MIN_ORDER = 0;

/** Max discount percent allowed (sanity cap). */
export const MAX_DISCOUNT_PERCENT = 100;

/** Default promo code expiry window (days from creation). */
export const DEFAULT_PROMO_VALID_DAYS = 30;
